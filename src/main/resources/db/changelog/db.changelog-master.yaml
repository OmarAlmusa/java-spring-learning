# db/changelog/db.changelog-master.yaml
# This is the master changelog file that Liquibase will use to track all database changes.

databaseChangeLog:
  - include:
      file: db.changelog-1.0.yaml
      relativeToChangelogFile: true